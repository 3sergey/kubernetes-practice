apiVersion: v1
kind: Service
metadata:
  name: nfs-test-service
  namespace: nfs-test
  labels:
    app: nfs-test-deployment
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: nfs-test-deployment